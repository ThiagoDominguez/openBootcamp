let nombre = "Thiago";

let apellido = "Dominguez";

const persona = {
  nombre,
  apellido,
};

// sessionStorage.setItem("persona", JSON.stringify(persona));

// localStorage.setItem("persona", JSON.stringify(persona));

// document.cookie = "persona=" + JSON.stringify(persona);

// document.cookie = "nombre-caducidad=nombre;expires=" + new Date().toUTCString();

// document.cookie =
//   "nombre-caducidad=nombre;expires=" +
//   new Date(2022, 11, 12, 18, 46).toUTCString();
